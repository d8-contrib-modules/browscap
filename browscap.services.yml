services:
  browscap:
    class: Drupal\browscap\BrowscapService
  cache.browscap:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory_method: cache_factory:get
    arguments: [browscap]